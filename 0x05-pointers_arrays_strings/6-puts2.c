#include "main.h"

/**
 * puts2 - prints every other character of a string,
 * starting with the first character
 *
 * @str: string
 *
 * Return: void
 */

void puts2(char *str)
{
int i, strl;

strl = 0;

while (str[strl] != '\0')
{
strl++
}
for (i = 0; i < len; i += 2)
{
_putchar(str[i]);
}
_putchar('\n');
}
