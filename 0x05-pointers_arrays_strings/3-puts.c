#include "main.h"

/**
 * _puts - prints a string followed by a new line..
 *
 *@str: the string to print.
 *
 */

void _puts(char *str)
{
int i;
char j = str[i];

for (i = 0; j != '\0'; i++)
{
_putchar(j);
}
_putchar('\n');
}
