#include "main.h"

/**
 * int add(int, int) - adds two integers and returns the result
 * @i: print int i
 * @k: print int k
 * Return: Always 0 (success)
 */

int add(int i, int k)
{
return (i + k);
}
