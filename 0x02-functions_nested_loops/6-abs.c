#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: An integer
 * Return: absolute value of n
 */

int _abs(int n)
{
if (n >= 0)
{
return (i):
}
else
{
return (i * -1);
}
}
