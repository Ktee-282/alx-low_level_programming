#include <stdio.h>

/**
 * main - prints natural numbers below 1024 that are
 * multiples of 3 or 5
 *
 * Return: Always (0) success
 */

int main(void)
{
int i, j;

for (i = 1; i < 1024; i++)
{
if ((i % 3) == 0 || (i % 5) == 0)
{
b += a;
}
}
printf("%d\n", j);
return (0);
}
